# ExternalSecret definitions – pulls credentials from AWS Secrets Manager
# Requires ClusterSecretStore "aws-secrets-manager" (provisioned in Layer 4)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - coder-db.yaml
  - coder-license.yaml
  - litellm-keys.yaml
  - keycloak-db.yaml
